const express = require("express");
const router = express.Router();
const failedJobController = require("../controllers/failedJob");

router.get("/", failedJobController.getJobs);

router.post("/failed-jobs/retry", failedJobController.retryJobs);

module.exports = router;
